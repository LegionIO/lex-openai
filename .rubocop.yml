inherit_gem:
  rubocop-legion: config/lex.yml

# OpenAI API methods require many keyword args; n: is a standard OpenAI param name
Metrics/ParameterLists:
  Max: 12

Naming/MethodParameterName:
  AllowedNames:
    - n
